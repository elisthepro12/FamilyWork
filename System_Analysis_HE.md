# ניתוח מערכת - FamilyWork

### 1. תיאור תחום הידע
אפליקציית **FamilyWork** נועדה לניהול וסנכרון מטלות הבית ורשימות קניות בין בני משפחה בזמן אמת. המערכת מאפשרת חלוקת תחומי אחריות, מעקב אחר ביצוע משימות, וניהול מלאי משותף. המטרה המרכזית היא לייעל את התנהלות משק הבית ולמנוע כפילויות או שכחה של מצרכים ומטלות, תוך שימוש במסד נתונים בענן המעדכן את כל המכשירים המחוברים לאותו קוד משפחתי.

### 2. אובייקטים נחוצים
*   **משימה (FamilyTask):** מייצג מטלה בבית (למשל: "שטיפת כלים").
*   **פריט קנייה (ShoppingItem):** מייצג מוצר שצריך לקנות או קיים במלאי.
*   **משתמש (User):** מייצג בן משפחה המשתמש באפליקציה.
*   **מנהל נתונים (Firebase Manager):** אחראי על התקשורת מול ה-Database.

### 3. סוגי נתונים
*   **String:** מזהים ייחודיים (ID), שמות משימות, קטגוריות, קוד משפחה, שמות משתמשים.
*   **boolean:** לציון האם משימה הושלמה (`isFinished`) או האם משימה היא יומית (`isDaily`).
*   **ArrayList:** לשמירת רשימות הדינמיות של המשימות והפריטים להצגה ב-RecyclerView.
*   **Map / HashMap:** לניהול רשימת המשתמשים המשויכים לכל משימה וסטטוס הביצוע שלהם.
*   **FirebaseDatabase & DatabaseReference:** לקישור וסנכרון הנתונים מול הענן בזמן אמת.
*   **Integer:** לייצוג צבעים (Color) של משימות או כמויות של מוצרים.
*   **Long:** לשמירת חותמות זמן (Timestamp) לצורך מעקב היסטורי.

### 4. ייצוג מידע
**באובייקט משימה (FamilyTask) בחרתי להשתמש בייצוג מידע על פי המפורט להלן:**
*   **String:** מייצג את שם המשימה והקטגוריה אליה היא שייכת. נתון זה מאפשר למיין את המשימות ולהציג אותן בצורה מאורגנת למשתמש.
*   **boolean:** משמש לניהול מצב המשימה (בוצעה/לא בוצעה), מה שמאפשר לסנן משימות מהרשימה הראשית לאחר סיומן.
*   **Map:** משמש לשמירת המשתמשים האחראיים על המשימה, כך שניתן לדעת מי מכל בני המשפחה צורך לבצע את המטלה.
*   **int:** מייצג את הצבע הייחודי המשויך למשימה או למשתמש לצורך זיהוי ויזואלי מהיר ב-UI.

**באובייקט משתמש (User) בחרתי להשתמש בייצוג מידע על פי המפורט להלן:**
*   **String:** מייצג את שם המשתמש, מספר הטלפון וקוד המשפחה אליו הוא משויך. מספר הטלפון משמש כמפתח ייחודי לזיהוי.

**באובייקט מנהל נתונים בחרתי להשתמש בייצוג נתונים על פי המפורט להלן:**
*   **FirebaseDatabase:** כדי להתחבר לשרתי גוגל ולאפשר סנכרון נתונים חי בין כל בני המשפחה.
*   **DatabaseReference:** כדי לגשת למיקומים ספציפיים בתוך עץ הנתונים (כמו "Tasks" או "ShoppingList") ולבצע פעולות קריאה וכתיבה.
*   **SharedPreferences:** לשמירת פרטי ההתחברות של המשתמש מקומית על המכשיר, כדי למנוע צורך בהתחברות חוזרת בכל הפעלה.

### 5. תיאור פעולות על המידע
*   **יצירת קבוצה:** הגרלת קוד משפחתי ייחודי ושמירתו ב-Firebase.
*   **הוספת משימה:** יצירת אובייקט `FamilyTask` ושליחתו למסד הנתונים תחת הקוד המשפחתי.
*   **עדכון סטטוס משימה:** שינוי ערך ה-`boolean` ב-Firebase הגורם לעדכון מיידי אצל כל בני המשפחה.
*   **ניהול רשימת קניות:** הוספה, עריכה ומחיקה של מוצרים בזמן אמת.
*   **הקראת רשימה (TTS):** הפיכת נתוני הטקסט של המוצרים לפלט קולי.
*   **מחיקת היסטוריה:** הסרת פריטים ישנים מההיסטוריה לאחר פרק זמן מוגדר.
