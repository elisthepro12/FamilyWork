# ניתוח מערכת - FamilyWork

### תיאור תחום הידע
אפליקציית **FamilyWork** היא כלי ניהול משותף שנועד לעזור לבני משפחה לארגן את חיי הבית במקום אחד. המערכת מאפשרת סנכרון בזמן אמת של מטלות ביתיות, רשימות קניות והיסטוריית רכישות בין מכשירים שונים. האפליקציה פותרת בעיות של שכחת מטלות או קנייה כפולה של מוצרים על ידי שימוש במסד נתונים בענן, ומספקת כלים ויזואליים וקוליים לייעול ההתנהלות המשפחתית.

### אובייקטים נחוצים
*   **משימה (FamilyTask)**
*   **פריט קנייה (ShoppingItem)**
*   **משתמש (User)**
*   **מנהל נתונים (FirebaseDatabase & SharedPreferences)**

### סוגי נתונים
*   **String:** מזהים ייחודיים (ID), שמות משימות, קטגוריות, קוד משפחה, שמות משתמשים.
*   **boolean:** לציון האם משימה הושלמה (`isFinished`) או האם היא יומית (`isDaily`).
*   **int:** לייצוג צבעים ייחודיים למשתמשים וכמויות של מוצרים.
*   **long:** לשמירת חותמות זמן (Timestamp) לניהול היסטוריית קניות.
*   **ArrayList:** לשמירת רשימות דינמיות של משימות ופריטים לתצוגה ב-Adapters.
*   **Map / HashMap:** לשמירת רשימת המשתמשים המשויכים לכל משימה וקישורם לסטטוס הביצוע.
*   **FirebaseDatabase:** לחיבור למסד הנתונים בענן.
*   **DatabaseReference:** לקישור לנתיבי מידע ספציפיים ב-Firebase.

### ייצוג מידע

**באובייקט משימה (FamilyTask) בחרתי להשתמש בייצוג מידע על פי המפורט להלן:**
*   **String:** מייצג את שם המשימה והקטגוריה אליה היא שייכת (למשל: "מטבח", "גינה"). בחרתי בנתון זה כי הוא מאפשר למיין את המשימות ולהציגן תחת כותרות ב-RecyclerView בצורה מאורגנת.
*   **boolean:** בעזרת נתון זה אני קובעת האם המשימה בוצעה או האם היא משימה שצריכה להופיע בכל יום מחדש. זה מאפשר למערכת לנהל את תצוגת ה-UI בצורה דינמית.
*   **int:** משמש לשמירת קוד הצבע של המשימה. בחרתי בנתון זה כדי להעניק למשתמש זיהוי ויזואלי מהיר של המשימות המשויכות אליו או לקטגוריה מסוימת.
*   **Map:** כדי לשמור את רשימת בני המשפחה האחראיים על המשימה. השימוש ב-Map מאפשר לבדוק במהירות (O(1)) האם משתמש מסוים משויך למשימה ולעדכן את הסטטוס שלו.

**באובייקט פריט קנייה (ShoppingItem) בחרתי להשתמש בייצוג מידע על פי המפורט להלן:**
*   **String:** מייצג את שם המוצר והתיאור שלו, וכן את שם קובץ התמונה (אם קיימת) המאוחסנת ב-Storage.
*   **long:** מכיוון שבעזרת נתון זה אני שומרת את זמן הוספת הפריט למערכת. נתון זה מאפשר לי לבצע חישוב זמן ולמחוק פריטים מההיסטוריה באופן אוטומטי לאחר שבעה ימים.
*   **int:** מייצג את הכמות הנדרשת מהמוצר.

**באובייקט משתמש (User) בחרתי להשתמש בייצוג מידע על פי המפורט להלן:**
*   **String:** מייצג את השם הפרטי, שם המשפחה, מספר הטלפון וקוד המשפחה. מספר הטלפון משמש כמפתח ייחודי לזיהוי המשתמש בכניסה לאפליקציה.

**באובייקט מנהל נתונים בחרתי להשתמש בייצוג נתונים על פי המפורט להלן:**
*   **FirebaseDatabase:** כדי להתחבר ל-Firebase (שם שמורים כל הנתונים) ולאפשר סנכרון חי בין כל בני המשפחה.
*   **DatabaseReference:** כדי לקשר לנתונים ספציפיים (Keys) ב-FirebaseDatabase, כמו רשימת המשימות או רשימת הקניות של משפחה מסוימת לפי הקוד שלה.
*   **SharedPreferences:** כדי לשמור את פרטי המשתמש והקוד המשפחתי מקומית על המכשיר, מה שמאפשר "כניסה אוטומטית" (Auto-Login) ללא צורך בהזנת פרטים בכל פעם.

### תיאור פעולות על המידע
יצירת קבוצה משפחתית חדשה (הגרלת קוד), הצטרפות לקבוצה קיימת באמצעות קוד, הוספת משימה חדשה, עדכון פרטי משימה או מחיקתה, סימון משימה כבוצעה, הוספת מוצר לרשימת קניות, עדכון מלאי, העברת פריטים להיסטוריה, השמעת רשימת הקניות באמצעות מנוע TTS (Text-to-Speech), והצגת סטטיסטיקות שבועיות של ביצוע מטלות.
