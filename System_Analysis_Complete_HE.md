# תירוח תחום הידע - FamilyWork

### אובייקטים נחוצים
*   **משימה (FamilyTask)**
*   **פריט קנייה (ShoppingItem)**
*   **משתמש (User)**
*   **מנהל נתונים (FirebaseDatabase & DatabaseReference)**

### סוגי נתונים
*   **String**
*   **boolean**
*   **int**
*   **long**
*   **ArrayList**
*   **Map / HashMap**
*   **FirebaseDatabase**
*   **DatabaseReference**

### ייצוג מידע

**באובייקט משימה (FamilyTask) בחרתי להשתמש בייצוג מידע על פי המפורט להלן:**
*   **String:** מייצג את שם המשימה ("taskName"), את שם המשתמש האחראי ("assignedTo") ואת היום בשבוע ("dayOfWeek"). בחרתי ב-String מכיוון שמדובר במידע טקסטואלי המוצג למשתמש ומאפשר זיהוי מהיר של המטלה.
*   **int:** מייצג את הצבע הייחודי ("color") המשויך למשימה או למשתמש. בחרתי בנתון זה כדי לאפשר הצגה ויזואלית צבעונית ב-RecyclerView המפרידה בין סוגי משימות או אחראים.
*   **boolean:** משמש לציון האם המשימה הושלמה או האם היא משימה יומית חוזרת (isDaily), מה שמאפשר לסנן את התצוגה.
*   **Map:** משמש לשמירת רשימת המשתמשים המשויכים למשימה ב-Firebase, כדי לאפשר גישה מהירה לפי מפתח (Key) של שם המשתמש.

**באובייקט פריט קנייה (ShoppingItem) בחרתי להשתמש בייצוג מידע על פי המפורט להלן:**
*   **String:** מייצג את שם המוצר והקטגוריה שלו.
*   **long:** משמש לשמירת זמן הוספת הפריט (Timestamp), מה שמאפשר לי למחוק פריטים מההיסטוריה באופן אוטומטי לאחר 7 ימים.
*   **int:** מייצג את כמות המוצר הנדרשת.

**באובייקט משתמש (User) בחרתי להשתמש בייצוג מידע על פי המפורט להלן:**
*   **String:** מייצג את שם המשתמש, מספר הטלפון שלו וקוד המשפחה (6 תווים). מספר הטלפון משמש כמזהה ייחודי לכניסה למערכת.

**באובייקט מנהל נתונים בחרתי להשתמש בייצוג נתונים על פי המפורט להלן:**
*   **Map:** כדי לשמור את כל אובייקטי המשימות והקניות שמתקבלים מה-Firebase ולשלוף אותם על פי ה-ID שלהם.
*   **FirebaseDatabase:** כדי ליצור חיבור רציף לשרת ולאפשר עדכון נתונים בזמן אמת לכל בני המשפחה.
*   **DatabaseReference:** כדי לקשר לנתונים הספציפיים (Key) של כל משפחה בנפרד (תחת הקוד המשפחתי).

### תיאור פעולות על המידע
הוספת משימה, מחיקת משימה, עדכון סטטוס משימה (בוצע/לא בוצע), יצירת קבוצה משפחתית (הגרלת קוד), הצטרפות לקבוצה קיימת, הוספת מוצר לרשימת קניות, מחיקת מוצר, העברת מוצר להיסטוריית קניות, השמעת רשימת הקניות (TTS), הצגת היסטוריית קניות שבועית.
