# תיאור תחום הידע - אפליקציית FamilyWork

### תיאור כללי
אפליקציית **FamilyWork** היא כלי לניהול וארגון חיי הבית והמשפחה במרחב משותף אחד. מטרת האפליקציה היא לסנכרן מטלות (Tasks), רשימות קניות (Shopping Lists) והיסטוריית רכישות בין מכשירים שונים של בני אותה משפחה בזמן אמת. האפליקציה פותרת בעיות נפוצות כמו שכחת מטלות ביתיות או קנייה כפולה של מצרכים, על ידי שימוש במסד נתונים בענן המעדכן את כל המשתמשים בו-זמנית.

### אובייקטים נחוצים
*   **משימה (FamilyTask):** מייצג מטלה בבית.
*   **פריט קנייה (ShoppingItem):** מייצג מוצר ברשימת הקניות או במלאי.
*   **משתמש (User):** מייצג בן משפחה רשום במערכת.
*   **מנהל נתונים (Firebase / SharedPreferences):** אחראי על סנכרון ושמירת המידע בענן ובאופן מקומי.

### סוגי נתונים
*   **String:** מזהים ייחודיים (ID), שמות משימות, קטגוריות, שמות משתמשים, מספרי טלפון וקוד משפחה.
*   **boolean:** לציון האם משימה הושלמה (`isFinished`) והאם משימה היא יומית וחוזרת (`isDaily`).
*   **ArrayList:** לשמירת רשימות של משימות ופריטי קנייה להצגה ב-Adapters.
*   **Map / HashMap:** לניהול רשימת המשתמשים המשויכים לכל משימה (התאמה בין שם המשתמש לסטטוס הביצוע).
*   **FirebaseDatabase & DatabaseReference:** לחיבור וקישור לנתונים ב-Firebase.
*   **Integer / int:** לייצוג צבעי משימות (Color) וכמויות של מוצרים.
*   **Long:** לשמירת זמן הוספת פריטים (Timestamp) לצורך ניהול היסטוריה.

### ייצוג מידע

**באובייקט משימה (FamilyTask) בחרתי להשתמש בייצוג מידע על פי המפורט להלן:**
*   **String:** מייצג את כותרת המשימה, הקטגוריה (כמו "מטבח" או "כלב") וה-ID הייחודי שלה. נתון זה מאפשר למיין את המשימות ולהציג אותן תחת כותרות מתאימות.
*   **boolean:** משמש לניהול מצב המשימה (בוצעה/לא בוצעה) וסוג המשימה (יומית/חד פעמית). זה מאפשר למערכת לדעת אילו משימות להציג ואילו למחוק או לאפס בכל יום.
*   **Map:** משמש לשמירת רשימת המשתמשים האחראיים על המשימה. בחירה ב-Map מאפשרת לבדוק במהירות האם משתמש מסוים משויך למשימה ולעדכן את הסטטוס שלו ללא צורך בלולאות מורכבות.

**באובייקט פריט קנייה (ShoppingItem) בחרתי להשתמש בייצוג מידע על פי המפורט להלן:**
*   **String:** מייצג את שם המוצר והתיאור שלו.
*   **int:** מייצג את כמות המוצר הנדרשת לקנייה.
*   **long:** משמש לשמירת זמן הקנייה. בעזרת נתון זה האפליקציה מחשבת מתי עברו 7 ימים כדי למחוק את הפריט מההיסטוריה באופן אוטומטי.

**באובייקט משתמש (User/Login) בחרתי להשתמש בייצוג מידע על פי המפורט להלן:**
*   **String:** מייצג את שם המשתמש, מספר הטלפון, שם המשפחה וקוד המשפחה (6 תווים). מספר הטלפון משמש כמפתח ייחודי לזיהוי המשתמש.

**באובייקט מנהל נתונים בחרתי להשתמש בייצוג נתונים על פי המפורט להלן:**
*   **FirebaseDatabase:** כדי לאפשר סנכרון מיידי בין כל בני המשפחה – ברגע שאחד מעדכן משימה או קנייה, כולם רואים זאת מיד.
*   **SharedPreferences:** כדי לשמור את נתוני ההתחברות (קוד וטלפון) מקומית בזיכרון הטלפון, כך שהמשתמש לא יצטרך להתחבר מחדש בכל פתיחה של האפליקציה.

### תיאור פעולות על המידע
*   **יצירת/הצטרפות למשפחה:** הגרלת קוד 6 תווים ייחודי או אימות קוד קיים מול Firebase.
*   **הוספת/עריכת משימה:** פתיחת דיאלוג המאפשר הזנת שם, קטגוריה ושיוך בני משפחה, ושמירתם כאובייקט ב-Database.
*   **סימון משימה כבוצעה:** עדכון שדה ה-boolean ב-Firebase המשתקף מיד אצל שאר בני המשפחה.
*   **ניהול רשימת קניות:** הוספת מוצרים, עדכון כמויות ומחיקת פריטים שנקנו.
*   **הקראת רשימה (TTS):** שימוש במנוע Text-to-Speech כדי להקריא את רשימת הקניות למשתמש בזמן אמת.
*   **ניהול היסטוריה:** העברת פריטים שנקנו לרשימת היסטוריה ומחיקתם האוטומטית לאחר שבוע ימים.
